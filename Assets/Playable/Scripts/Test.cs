using UnityEngine;

public class TimeScaleDebugger : MonoBehaviour
{
    private float timer;

    void Update()
    {
        //int i = (int)(1 / Time.deltaTime);
        //if (i > 45 && i < 100) Debug.Log("60");
        //else Debug.Log(i);
    }
}
